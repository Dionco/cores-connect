<!-- intent-skills:start -->
# Skill mappings - when working in these areas, load the linked skill file into context.
skills:
  - task: "M365 onboarding and account provisioning automation"
    # To load this skill, run: npx @tanstack/intent@latest list | grep msgraph
  - task: "Debugging Microsoft Graph permissions and scopes"
    # To load this skill, run: npx @tanstack/intent@latest list | grep msgraph
  - task: "Optimizing Supabase Postgres queries and indexes"
    # To load this skill, run: npx @tanstack/intent@latest list | grep supabase-postgres-best-practices
  - task: "Designing migrations from mock data to persisted database tables"
    # To load this skill, run: npx @tanstack/intent@latest list | grep supabase-postgres-best-practices
<!-- intent-skills:end -->